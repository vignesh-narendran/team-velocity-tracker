import { useState, useEffect } from 'react'; import axios from 'axios'; import { API_URL } from '../store'; import { Card, CardHeader, CardTitle, CardContent } from '../components/ui/card'; import { Button } from '../components/ui/button'; import { Input } from '../components/ui/input'; import { Label } from '../components/ui/label'; export default function DsuView() { const [members, setMembers] = useState<any[]>([]); const [activeStories, setActiveStories] = useState<any[]>([]); const [formData, setFormData] = useState({ memberId: '', storyId: '', statusUpdate: '', targetCompletion: '', isBlocked: false }); const [selectedStory, setSelectedStory] = useState<any>(null); const fetchData = async () => { const [meRes, stRes] = await Promise.all([ axios.get(API_URL+'/members'), axios.get(API_URL+'/stories'), ]); setMembers(meRes.data); setActiveStories(stRes.data.filter((s: any) => !['complete', 'done'].includes(s.stage.toLowerCase()))); }; useEffect(() => { fetchData(); }, []); useEffect(() => { if (formData.storyId) { const story = activeStories.find(s => s.id === parseInt(formData.storyId)); setSelectedStory(story); if (story && !formData.targetCompletion) { setFormData(prev => ({ ...prev, targetCompletion: story.proposedEnd.split('T')[0] })); } } else { setSelectedStory(null); } }, [formData.storyId, activeStories, formData.targetCompletion]); const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); await axios.post(API_URL+'/dsus', { ...formData, memberId: parseInt(formData.memberId), storyId: parseInt(formData.storyId), }); alert('DSU Update Recorded!'); setFormData({ memberId: '', storyId: '', statusUpdate: '', targetCompletion: '', isBlocked: false }); }; return ( <div className="max-w-2xl mx-auto space-y-6"> <Card> <CardHeader> <CardTitle>Daily Status Update</CardTitle> </CardHeader> <CardContent> <form onSubmit={handleSubmit} className="space-y-4"> <div className="grid gap-2"> <Label>Who are you?</Label> <select required className="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm" value={formData.memberId} onChange={e => setFormData({ ...formData, memberId: e.target.value })}> <option value="">Select Member</option> {members.map(m => <option key={m.id} value={m.id}>{m.name}</option>)} </select> </div> <div className="grid gap-2"> <Label>Select Story</Label> <select required className="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm" value={formData.storyId} onChange={e => setFormData({ ...formData, storyId: e.target.value })}> <option value="">Select Task</option> {activeStories.map(s => <option key={s.id} value={s.id}>{s.storyNumber} - {s.name}</option>)} </select> </div> {selectedStory && ( <div className="p-4 bg-slate-100 dark:bg-slate-800 rounded text-sm"> <p><strong>Originally Proposed End:</strong> {new Date(selectedStory.proposedEnd).toLocaleDateString()}</p> <p><strong>Stage:</strong> {selectedStory.stage}</p> </div> )} <div className="grid gap-2"> <Label>Target Completion Date</Label> <Input type="date" required value={formData.targetCompletion} onChange={e => setFormData({ ...formData, targetCompletion: e.target.value })} /> </div> <div className="grid gap-2"> <Label>Status Update / Comments</Label> <Input required value={formData.statusUpdate} onChange={e => setFormData({ ...formData, statusUpdate: e.target.value })} /> </div> <div className="flex items-center gap-2 mt-4"> <input type="checkbox" id="isBlocked" checked={formData.isBlocked} onChange={e => setFormData({ ...formData, isBlocked: e.target.checked })} /> <Label htmlFor="isBlocked">I am blocked on this task</Label> </div> <Button type="submit" className="w-full">Submit Update</Button> </form> </CardContent> </Card> </div> ); }